{
	"Servers": {
		"1": {
			"Name": "${DB_NAME}",
			"Group": "Servers",
			"Host": "${DB_HOST}",
			"Port": 5432,
			"MaintenanceDB": "${DB_MAINTENANCE}",
			"Username": "${POSTGRES_USER}",
			"Password": "${POSTGRES_PASSWORD}",
			"SSLMode": "prefer",
			"Comment": "Repen jutut!"
		}
	}
}